#include <steg.h>

int steg_extract(stegobmp_configuration_ptr config)
{
    // if (config->steg_algo = "LSB1"){

    // }
    // else if (config->steg_algo = "LSB4"){

    // }
    // else if (config->steg_algo = "LSBI"){

    // }
    return 0;
}
